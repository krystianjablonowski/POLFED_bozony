Place the comparison CSV files here before the production analysis:

- `entropy_peak_points.csv`
- `peaks_L7_corrected.csv`

They are read only during `analyze`; they do not affect the covariance
predictor itself.
